<%= form_for(@sports_installation) do |f| %>
  <% if @sports_installation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sports_installation.errors.count, "error") %> prohibited this sports_installation from being saved:</h2>

      <ul>
      <% @sports_installation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :installation_id %><br>
    <%= f.number_field :installation_id %>
  </div>
  <div class="field">
    <%= f.label :sport_id %><br>
    <%= f.number_field :sport_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
